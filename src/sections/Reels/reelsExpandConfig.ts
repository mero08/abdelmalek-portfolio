/** Locked expand overlay parameters — match approved v1a prototype. */
export const REELS_EXPAND = {
  CLOSE_MS: 480,
  FLIP_MS: 500,
  FLIP_EASING: 'cubic-bezier(0.22, 1, 0.36, 1)',
  MAX_HEIGHT: 720,
  VIEWPORT_HEIGHT_RATIO: 0.78,
  VIEWPORT_EDGE_PAD: 16,
  VERTICAL_OFFSET: 20,
  OVERLAY_Z: 200,
  TAP_MOVE_PX: 12,
  TAP_MAX_MS: 350,
  SCRUB_STEPS: 1000,
} as const
